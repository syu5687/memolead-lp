# @version v0002 | 2026-08-07 | メモリード福岡 おせち申込フォーム Worker
name = "memolead-fukuoka-osechi"
main = "worker.js"
compatibility_date = "2026-08-01"

# 通知先・送信元は worker.js 冒頭の CONFIG で管理します（vars 不要）。
# 秘密情報は必ずシークレットで登録：
#   npx wrangler secret put BREVO_API_KEY
