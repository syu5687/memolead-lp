# 2026-09-24 ブライダルリング 申込フォーム新設 報告

## 依頼
memolead-lp（/public/）内にブライダルリング専用申込フォームを新設。項目：氏名／住所／連絡先／参加希望日時（ドロップダウンorチェック）／結婚予定（年月ドロップダウン＋未定）／特に当日相談したいこと。

## 実施内容
- 新規フォルダ `public/bridal-ring/` を作成（既存ファイルの変更なし）
  - index.html（v0001）／worker.js（v0001）／wrangler.toml／.htaccess／README.md
- 構成は既存おせちフォームと同じ標準：Cloud Run ＋ Cloudflare Worker ＋ Brevo
- 参加希望日時：チェックボックス（複数選択可）を採用。第1希望だけだと日程が合わない時に再調整の手間が増えるため。日付ごとに時間枠カード表示、満席指定・過去日自動非表示に対応
- 結婚予定：今月〜24か月先＋「◯年◯月以降」＋「未定」を自動生成（毎月の更新作業不要）
- 連絡先：電話・メールとも必須（メールは自動返信に使用）
- 郵便番号→住所自動入力（zipcloud JSONP）
- GTM-KG9CZ8Q4 設置、送信成功時に `bridal_ring_submit` を dataLayer へ送信

## 検証結果
- Playwright（PC 1200px／SP 390px）で表示確認OK
- 未選択・メール形式不正時のエラー表示OK、送信ペイロード正常
- Worker をBrevoモックで実行：担当者通知・自動返信の2通生成、改行・HTMLエスケープ正常
- 本番送信テストは未実施（こちらの環境から外部送信不可。ご自身のPCでcurl／実フォームで実施）

## 未確認・要対応
| 優先 | 内容 |
|---|---|
| 済 | 参加希望日時：10/4(日)・10/10(土)・10/18(日)・10/24(土) 各13:00/15:00/17:00（約1時間）を反映（index.html v0002） |
| S | 通知先メール（現在 mk@emanet.jp。会場担当の宛先） |
| S | 開催会場名・住所（フォーム見出し・自動返信に記載するか） |
| S | Worker デプロイ＋`BREVO_API_KEY` 登録 → GitHub Desktop で push |
| A | GTMで `bridal_ring_submit` → GA4 generate_lead／広告CV 設定 |
| B | 申込一覧が必要ならD1保存＋管理画面（おせちと同方式） |

## ダメ出し検証
- 当日の過ぎた時間枠はその日の終わりまで表示される → 当日申込があれば担当者が電話で調整
- 通知先が会場担当に届かない → 対応漏れ。担当宛先の確定が必要
- ブランド表記を「メモリード」のみにしている → 会場名が決まれば見出し・フッター・自動返信に反映

## 追記 10:14 開催日程の反映
- 10/4(日)・10/10(土)・10/18(日)・10/24(土)、各日 13:00〜14:00／15:00〜16:00／17:00〜18:00 の計12枠
- 「所要時間は各回約1時間です」を日時欄に記載
- 曜日はカレンダーで確認済み。PC/SP表示・送信テストOK
- 通知先・会場名は引き続き未確認

## 追記 10:19 会場情報の反映
- 会場：サロン・ド・ルシェル／佐賀県佐賀市多布施2丁目15-1／TEL 0952-20-1516
- フォーム：見出し下に会場・住所（Googleマップへのリンク）・電話（タップで発信）、フッターにも同じ内容。title を「｜サロン・ド・ルシェル」に変更（index.html v0003）
- メール（worker.js v0002）：送信者名・自動返信の件名を「サロン・ド・ルシェル」に変更。担当者通知と自動返信の両方に会場の枠（住所は地図リンク）を追加。自動返信に「ご不明な点や日時の変更は、上記お電話までご連絡ください」を追加
- スマホで「必須」ラベルが縦に折れる点を修正
- Workerを変更したため、デプロイ時は `npx wrangler deploy` が必要（未デプロイなので初回デプロイで反映）
- 未確認：通知先メール（現在 mk@emanet.jp のみ）
